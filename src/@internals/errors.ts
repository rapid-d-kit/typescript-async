import { ErrorCode, Exception, ERROR_CODE, errorDescription, isKnownError } from '@rapid-d-kit/safe';


// ErrorCode.extend([ ]);


export { ErrorCode, Exception, ERROR_CODE, errorDescription, isKnownError };
